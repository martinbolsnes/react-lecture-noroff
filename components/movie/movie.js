/* import react, { useState } from 'react';

const MovieSelector = (initMovie) => {
  const [movie, setmovie] = useState(initMovie);
  setmovie('2012');
  return <div>{movie}</div>;
};

export default MovieSelector; */
